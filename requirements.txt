# ── Core data ─────────────────────────────────────────────
duckdb>=0.10
pandas>=2.0
numpy>=1.26
scipy>=1.13

# ── Ingestion (scraper) ───────────────────────────────────
requests>=2.31
python-dotenv>=1.0          # loads the metering cookie from .env

# ── Simulation (Mesa agent-based model) ───────────────────
mesa==2.3.4                 # pinned: 3.x changed the Agent/scheduler API

# ── Reinforcement learning (Phase 6) ──────────────────────
gymnasium==1.3.0
stable-baselines3==2.9.0    # installs a compatible torch automatically

# ── Plotting (all notebooks) ──────────────────────────────
matplotlib>=3.7
seaborn>=0.12
pillow>=10.0                # validation/combine_calibration.py

# ── Notebook runtime (local notebooks 00–03) ──────────────
jupyter>=1.0